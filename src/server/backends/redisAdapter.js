// export const kvBackend = {
// const kvBackend = {
//     async scanSample({ match, count }) { /* SCAN ... TYPE ... */ },
//     async applyAcl({ username, secret, commands, patterns }) { /* ACL SETUSER */ },
//     async testAcl({ username, secret, commands, patterns }) { /* tmp user + GET/SET */ },
// };
